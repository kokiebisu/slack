mutation Register(
  $fullname: String!
  $email: String!
  $password: String!
  $avatarBackground: String!
) {
  register(
    email: $email
    password: $password
    fullname: $fullname
    avatarBackground: $avatarBackground
  ) {
    ok
    errorlog
  }
}
