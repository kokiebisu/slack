mutation CreateChannel(
  $name: String!
  $teamId: String!
  $description: String
  $isPublic: Boolean!
) {
  createChannel(
    name: $name
    teamId: $teamId
    description: $description
    isPublic: $isPublic
  ) {
    ok
  }
}
