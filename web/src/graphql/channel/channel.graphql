query GetChannelById($channelId: String!) {
  getChannelById(channelId: $channelId) {
    ok
    errorlog
    channel {
      id
      name
      topic
      isPublic
      fullname
      description
      createdAt
    }
  }
}
